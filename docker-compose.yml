version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: agent_postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secretpassword
      POSTGRES_DB: agent_memory
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant:latest
    container_name: agent_qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

  backend:
    build: .
    container_name: agent_backend
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --workers 4
    ports:
      - "8000:8000"
    environment:
      - POSTGRES_URI=postgresql://admin:secretpassword@postgres:5432/agent_memory
      - DATABASE_URL=postgresql://admin:secretpassword@postgres:5432/agent_memory
      - QDRANT_URL=http://qdrant:6333 
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - INNGEST_BASE_URL=http://host.docker.internal:8288
    depends_on:
      - postgres
      - qdrant
    restart: unless-stopped

  frontend:
    build: .
    container_name: agent_frontend
    command: streamlit run frontend/streamlit_app.py --server.port 8501 --server.address 0.0.0.0
    ports:
      - "8501:8501"
    environment:
      - API_BASE_URL=http://backend:8000
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  pgdata:
  qdrant_data: